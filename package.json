{
  "name": "@groupthinking/nostr-inference",
  "version": "0.1.0",
  "description": "Nostr registry-of-kinds full codegen + Inference Orchestration Layer (Edge-Cloud Continuum)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "echo \"Add tests later\" && exit 0",
    "lint": "eslint . --ext .ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "zod": "^3.24.0",
    "rx-nostr": "^2.0.0",
    "nostr-tools": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "prisma": "^5.15.0"
  },
  "keywords": ["nostr", "inference-orchestration", "edge-cloud", "registry-of-kinds", "zod", "rust-nostr"],
  "author": "groupthinking",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/groupthinking/nostr-Inference.git"
  }
}
