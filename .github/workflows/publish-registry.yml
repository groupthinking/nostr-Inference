name: Publish Registry as Nostr Event
on:
  push:
    paths: ['schema.json']
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Registry updated — new kind 30078 event ready"
